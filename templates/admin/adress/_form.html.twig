{{ form_start(form) }}
<div class="row">
    <div class="col-md-3">{{ form_row(form.adress_street_number) }}</div>
    <div class="col-md-9">{{ form_row(form.adress_street_name) }}</div>
</div>
<div class="row">
    <div class="col-md-9">{{ form_row(form.adress_information) }}</div>
    <div class="col-md-3">
        <div class="city">
            <p>{{ form_row(form.city.city_name) }}</p>
            <p>{{ form_row(form.city.city_postcode) }}</p>
            <p>{{ form_row(form.city.city_country) }}</p>
        </div>
    </div>
</div>

<button class="btn btn-success mr-2" >{{ button_label|default('Save') }}</button>
<a class="btn btn-primary" href="{{ path('event_index') }}">Retour à la liste</a>
{{ form_end(form) }}
