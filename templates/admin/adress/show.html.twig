{% extends 'base.html.twig' %}

{% block title %}Adress{% endblock %}

{% block body %}
    <h1>Adress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adress.id }}</td>
            </tr>
            <tr>
                <th>Adress_street_number</th>
                <td>{{ adress.adressStreetNumber }}</td>
            </tr>
            <tr>
                <th>Adress_street_name</th>
                <td>{{ adress.adressStreetName }}</td>
            </tr>
            <tr>
                <th>Adress_information</th>
                <td>{{ adress.adressInformation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adress_index') }}">back to list</a>

    <a href="{{ path('adress_edit', {'id': adress.id}) }}">edit</a>

    {{ include('admin/adress/_delete_form.html.twig') }}
{% endblock %}
