{% extends 'base.html.twig' %}

{% block nav %}
    {% include 'layout/nav_admin.html.twig' %}
{% endblock %}

{% block body %}
<div class="container-fluid section--box">
    <div class="row">

        <div class="col-lg-12 col-md-12 col-sm-12 xs-12">
            <div class="card text-dark p-5">
                <h1 class="pt-5 card-title">Ville</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>City_name</th>
                <td>{{ city.cityName }}</td>
            </tr>
            <tr>
                <th>City_postcode</th>
                <td>{{ city.cityPostcode }}</td>
            </tr>
            <tr>
                <th>City_country</th>
                <td>{{ city.cityCountry }}</td>
            </tr>
        </tbody>
    </table>

    <a  class="btn btn-default" href="{{ path('city_index') }}">back to list</a>

    <a  class="btn btn-primary" href="{{ path('city_edit', {'id': city.id}) }}">edit</a>

    {{ include('admin/city/_delete_form.html.twig') }}
{% endblock %}
