security:
    encoders:
        App\Entity\User:
            algorithm: auto

    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: lazy
            guard:
                authenticators:
                    - App\Security\LoginFormAuthenticator
            logout:
                path: app_logout
                # where to redirect after logout
                target: home

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

        secured_area:
            form_login:
                csrf_token_generator: security.csrf.token_manager

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    role_hierarchy:
        ROLE_STUDENT: [ROLE_USER]
        ROLE_TEACHER: [ROLE_STUDENT, ROLE_USER]
        ROLE_WRITER: [ROLE_USER]
        ROLE_ADMIN: [ROLE_WRITER, ROLE_TEACHER, ROLE_STUDENT, ROLE_USER]
        ROLE_SUPER_ADMIN: [ROLE_ADMIN, ROLE_WRITER, ROLE_TEACHER, ROLE_STUDENT, ROLE_USER]

    access_control:
        - { path: ^/super_admin, roles: ROLE_SUPER_ADMIN }
        - { path: ^/admin, roles: ROLE_WRITER }
        - { path: ^/profile, roles: ROLE_USER }
